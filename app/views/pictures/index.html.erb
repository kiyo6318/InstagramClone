<h1>投稿一覧</h1>

<% @pictures.each do |picture| %>
  <%= picture.user.name %>
  <%= image_tag(picture.image.url) %>
  <% if logged_in? %>
    <%= link_to "詳細",picture_path(picture.id) %>
  <% end %>
<% end %>